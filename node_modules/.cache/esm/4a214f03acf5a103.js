let express,fetch,cheerio;_acf‍.w("express",[["default",["express"],function(v){express=v}]]);_acf‍.w("node-fetch",[["default",["fetch"],function(v){fetch=v}]]);_acf‍.w("cheerio",[["default",["cheerio"],function(v){cheerio=v}]]);



const app = express();

app.get('/', async (req, res) => {
  try {
    const response = await fetch('https://www.legis.state.pa.us/');
    const html = await response.text();
    const $ = cheerio.load(html);
    const title = $('title').text();
    res.send(title);
  } catch (error) {
    _acf‍.g.console.error(error);
    res.status(500).send('Error retrieving data');
  }
});

app.listen(3000, () => {
  console.log('Server listening on port 3000');
});
